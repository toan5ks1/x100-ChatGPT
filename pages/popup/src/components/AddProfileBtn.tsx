import { Button } from '@extension/ui/lib/components';

const AddProfileBtn = () => {
  const addNewSlot = () => {};
  return <Button onClick={addNewSlot}>Add profile</Button>;
};

export default AddProfileBtn;
